=== INSTALL

1. Go to project build dir
cd <project_dir>/build

2. Install gulp and modules
npm install -g gulp
npm install

=== BUILDING

1. Go to project build dir
cd <project_dir>/build

2. Run build command
	